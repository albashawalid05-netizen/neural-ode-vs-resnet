seed: 42
device: cuda

data:
  n_sequences: 3000
  seq_len: 50
  dt: 0.1
  noise_std: 0.05
  missing_rate: 0.3
  train_split: 0.8

train:
  batch_size: 256
  epochs: 15
  lr: 1.0e-3

model:
  hidden_dim: 64

ode:
  solver: rk4
  rtol: 1.0e-4
  atol: 1.0e-4
